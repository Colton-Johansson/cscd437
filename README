To set up the input file correctly, place it in the same folder as /src is so the program can find it. NOT IN /SRC, but the so they share a folder
For security purposes, we wanted to limit which files it has access to read, that is why it is set up this way.
